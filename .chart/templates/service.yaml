apiVersion: v1
kind: Service
metadata:
  name: mongoose
spec:
  selector:
    app: mongoose
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
